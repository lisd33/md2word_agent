# Intermediate Outputs

This directory stores persisted Stage 3 model outputs.

Current subdirectories:

- `template_understanding/`: final structured JSON returned by the model for template-structure understanding, plus the normalized `TemplateRequirement` built from that response.

These files are generated automatically when Stage 3 runs through the CLI or HTTP API.
